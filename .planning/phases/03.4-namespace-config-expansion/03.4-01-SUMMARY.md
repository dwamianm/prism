---
phase: 03.4-namespace-config-expansion
plan: 01
subsystem: types
tags: [enum, scope, namespace, rfc-0004, extraction-prompt]

# Dependency graph
requires:
  - phase: 02.1-scope-isolation-fix
    provides: "3-member Scope enum with scope isolation across all storage layers"
provides:
  - "6-member Scope enum per RFC-0004 S3 (PERSONAL, PROJECT, ORGANISATION, AGENT, SYSTEM, SANDBOX)"
  - "Updated LLM extraction prompts with all 6 scope values"
  - "Updated schema field descriptions with all 6 scope values"
affects: [03.4-02, ingestion, retrieval, storage]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Clean enum rename (ORG -> ORGANISATION) with no migration aliases"

key-files:
  created:
    - tests/test_scope_expansion.py
  modified:
    - src/prme/types.py
    - src/prme/ingestion/extraction.py
    - src/prme/ingestion/schema.py
    - tests/test_retrieval_filter_forwarding.py

key-decisions:
  - "Clean break rename ORG to ORGANISATION with no backward-compat aliases per CONTEXT.md"
  - "SANDBOX docstring documents HARD_DELETE expiry action support (RFC-0004 S7)"

patterns-established:
  - "RFC-0004 6-member Scope enum: all new scope-aware code must handle all 6 values"

requirements-completed: [NSPC-01]

# Metrics
duration: 2min
completed: 2026-03-02
---

# Phase 03.4 Plan 01: Scope Enum Expansion Summary

**Scope enum expanded from 3 to 6 members (PERSONAL, PROJECT, ORGANISATION, AGENT, SYSTEM, SANDBOX) per RFC-0004 S3 with ORG->ORGANISATION rename and updated LLM extraction prompts**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-02T00:33:23Z
- **Completed:** 2026-03-02T00:35:52Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- Scope enum expanded to 6 members matching RFC-0004 S3 exactly
- ORG renamed to ORGANISATION (clean break, no migration)
- LLM extraction system prompt updated with all 6 scope descriptions
- ExtractedEntity and ExtractedFact field descriptions updated for all 6 scopes
- All Scope.ORG references eliminated from src/ and tests/
- All 67 tests pass including 20 new scope expansion tests

## Task Commits

Each task was committed atomically:

1. **Task 1: Expand Scope enum to 6 members and update LLM prompts** - `e852647` (test: RED), `e44a5b4` (feat: GREEN)
2. **Task 2: Update all Scope.ORG references across source and test files** - `5ea6d75` (fix)

_Note: Task 1 used TDD with separate test and implementation commits._

## Files Created/Modified
- `src/prme/types.py` - Scope enum expanded from 3 to 6 members with RFC-0004 docstring
- `src/prme/ingestion/extraction.py` - EXTRACTION_SYSTEM_PROMPT updated with 6 scope descriptions
- `src/prme/ingestion/schema.py` - ExtractedEntity.scope and ExtractedFact.scope field descriptions updated
- `tests/test_scope_expansion.py` - 20 tests for enum members, prompt content, schema descriptions
- `tests/test_retrieval_filter_forwarding.py` - Scope.ORG -> Scope.ORGANISATION in test_multi_scope_filter

## Decisions Made
- Clean break rename with no aliases per CONTEXT.md locked decision (pre-release, no production data)
- SANDBOX enum member documents HARD_DELETE support in docstring rather than as a separate constant (cleaner for this phase; enforcement constant can be added when retention policy is implemented in Phase 5)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Scope enum now has all 6 RFC-0004 namespace types
- Ready for Plan 02 (config nesting and [HYPOTHESIS] parameter wiring)
- All existing code that imports Scope works unchanged (only ORG references were updated)

## Self-Check: PASSED

All files verified present. All commit hashes verified in git log.

---
*Phase: 03.4-namespace-config-expansion*
*Completed: 2026-03-02*
