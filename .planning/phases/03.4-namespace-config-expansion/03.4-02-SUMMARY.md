---
phase: 03.4-namespace-config-expansion
plan: 02
subsystem: config
tags: [pydantic-settings, config, scoring-weights, confidence-matrix, env-vars]

# Dependency graph
requires:
  - phase: 03.1-epistemic-type-confidence-matrix
    provides: ConfidenceMatrix, EPISTEMIC_WEIGHTS, ScoringWeights, PackingConfig
  - phase: 03-retrieval-pipeline
    provides: RetrievalPipeline, scoring, filtering modules
provides:
  - PRMEConfig with scoring, packing, epistemic_weights, unverified_confidence_threshold, confidence_overrides fields
  - ConfidenceMatrix.with_overrides() merge method for config-driven matrix customization
  - Config wiring from PRMEConfig through MemoryEngine to RetrievalPipeline and IngestionPipeline
  - Environment variable support for all HYPOTHESIS parameters (PRME_SCORING__W_SEMANTIC, etc.)
affects: [04-organizer, 05-api, evaluation-harness]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Config + defaults pattern: module-level DEFAULT_* constants as fallbacks for standalone usage, PRMEConfig fields for wired paths"
    - "Nested pydantic-settings env vars: PRME_SCORING__W_SEMANTIC=0.25 parsed via env_nested_delimiter='__'"
    - "Optional parameter threading: functions accept None-defaulted config params, fall back to module constants"

key-files:
  created: [tests/test_config_expansion.py]
  modified:
    - src/prme/config.py
    - src/prme/epistemic/matrix.py
    - src/prme/storage/engine.py
    - src/prme/retrieval/scoring.py
    - src/prme/retrieval/filtering.py
    - src/prme/ingestion/pipeline.py

key-decisions:
  - "Direct import of ScoringWeights/PackingConfig in config.py (no circular dep exists) rather than lazy import, enabling pydantic-settings env var parsing"
  - "Optional parameter threading pattern: scoring.py, filtering.py accept None-defaulted config params with module constant fallback"
  - "MemoryEngine.__init__ accepts optional confidence_matrix/epistemic_weights/threshold for backward compat with direct construction"

patterns-established:
  - "Config + defaults: PRMEConfig provides wired values, module-level DEFAULT_* remain for standalone/test usage"
  - "Optional config threading: functions accept optional config override, default to module constant when None"

requirements-completed: [TRST-07]

# Metrics
duration: 6min
completed: 2026-03-02
---

# Phase 03.4 Plan 02: Config Expansion Summary

**PRMEConfig expanded with ScoringWeights, PackingConfig, epistemic weights, confidence threshold, and confidence matrix overrides -- all tunable via env vars (PRME_SCORING__W_SEMANTIC=0.25) without code changes**

## Performance

- **Duration:** 6 min
- **Started:** 2026-03-02T00:33:26Z
- **Completed:** 2026-03-02T00:39:44Z
- **Tasks:** 2
- **Files modified:** 7

## Accomplishments
- PRMEConfig expanded with 5 new fields: scoring (ScoringWeights), packing (PackingConfig), epistemic_weights (dict), unverified_confidence_threshold (float), confidence_overrides (dict with validation)
- ConfidenceMatrix.with_overrides() method for creating new frozen matrix instances with merged cell overrides
- Full config wiring: MemoryEngine.create() passes config to RetrievalPipeline (scoring/packing), IngestionPipeline (confidence_matrix), and stores epistemic_weights/threshold for store() method
- Environment variable support for all nested config: PRME_SCORING__W_SEMANTIC, PRME_PACKING__TOKEN_BUDGET, etc.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add config fields to PRMEConfig and ConfidenceMatrix.with_overrides()** (TDD)
   - `2f1e7c7` (test: RED - failing tests)
   - `d19d9a9` (feat: GREEN - implementation)
2. **Task 2: Wire config through MemoryEngine to retrieval and ingestion consumers** - `c889602` (feat)

## Files Created/Modified
- `src/prme/config.py` - Added ScoringWeights, PackingConfig, epistemic_weights, unverified_confidence_threshold, confidence_overrides fields with validation
- `src/prme/epistemic/matrix.py` - Added ConfidenceMatrix.with_overrides() merge method
- `src/prme/storage/engine.py` - Wired PRMEConfig to RetrievalPipeline and IngestionPipeline; store() uses instance confidence matrix
- `src/prme/retrieval/scoring.py` - compute_composite_score/score_and_rank accept optional epistemic_weights dict
- `src/prme/retrieval/filtering.py` - filter_epistemic accepts optional unverified_threshold parameter
- `src/prme/ingestion/pipeline.py` - IngestionPipeline accepts optional confidence_matrix, uses instance matrix for fact confidence lookup
- `tests/test_config_expansion.py` - 16 tests for PRMEConfig fields, validation, and ConfidenceMatrix.with_overrides()

## Decisions Made
- Direct import of ScoringWeights/PackingConfig in config.py rather than lazy import -- no circular dependency exists between config.py and retrieval/config.py, and direct import enables pydantic-settings env var nested delimiter parsing
- Optional parameter threading pattern: functions accept None-defaulted config params, fall back to module constants when None -- preserves backward compatibility for all callers
- MemoryEngine.__init__ accepts optional confidence_matrix/epistemic_weights/threshold parameters with module-level default fallback, preserving backward compat for direct construction in tests

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Fixed env var parsing for nested ScoringWeights/PackingConfig**
- **Found during:** Task 2 (config wiring verification)
- **Issue:** Plan suggested using `Any` type annotation with lazy import for scoring/packing fields; pydantic-settings cannot parse nested env vars (PRME_SCORING__W_SEMANTIC) when field type is Any
- **Fix:** Direct import of ScoringWeights/PackingConfig at module level (verified no circular dependency); proper type annotations enable pydantic-settings nested delimiter env var parsing
- **Files modified:** src/prme/config.py
- **Verification:** PRME_SCORING__W_SEMANTIC=0.25 correctly overrides default; all 67 tests pass
- **Committed in:** c889602

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Auto-fix required for correctness of env var support requirement. No scope creep.

## Issues Encountered
None beyond the env var parsing deviation documented above.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- All [HYPOTHESIS]-marked parameters from the audit inventory are now accessible through PRMEConfig
- TRST-07 (no systematic config mechanism) is closed
- Phase 03.4 complete: Scope enum expanded (Plan 01) and config wiring done (Plan 02)
- Ready for Phase 04 (Organizer) or other phases that need config-driven parameter tuning

## Self-Check: PASSED

All 7 claimed files verified present. All 3 commit hashes verified in git log.

---
*Phase: 03.4-namespace-config-expansion*
*Completed: 2026-03-02*
