---
phase: 03.4-namespace-config-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/prme/types.py
  - src/prme/ingestion/extraction.py
  - src/prme/ingestion/schema.py
  - tests/test_retrieval_filter_forwarding.py
autonomous: true
requirements:
  - NSPC-01

must_haves:
  truths:
    - "Scope enum has exactly 6 members: PERSONAL, PROJECT, ORGANISATION, AGENT, SYSTEM, SANDBOX"
    - "No references to Scope.ORG or the string 'org' as a scope value exist anywhere in src/ or tests/"
    - "LLM extraction prompts list all 6 scope values including 'organisation' (not 'org')"
    - "All existing tests pass after the rename"
  artifacts:
    - path: "src/prme/types.py"
      provides: "6-member Scope enum per RFC-0004 S3"
      contains: "ORGANISATION = \"organisation\""
    - path: "src/prme/ingestion/extraction.py"
      provides: "Updated extraction system prompt with 6 scope values"
      contains: "organisation"
    - path: "src/prme/ingestion/schema.py"
      provides: "Updated field descriptions with 6 scope values"
      contains: "organisation"
  key_links:
    - from: "src/prme/types.py"
      to: "all source files importing Scope"
      via: "Scope enum member name change"
      pattern: "Scope\\.ORGANISATION"
    - from: "src/prme/ingestion/extraction.py"
      to: "LLM extraction output"
      via: "system prompt scope values"
      pattern: "organisation.*organization-wide"
---

<objective>
Expand the Scope enum from 3 members (PERSONAL, PROJECT, ORG) to 6 members (PERSONAL, PROJECT, ORGANISATION, AGENT, SYSTEM, SANDBOX) per RFC-0004 Section 3, and update all references across the codebase.

Purpose: Close audit gap NSPC-01 (3/6 namespace types). The rename from ORG to ORGANISATION is a clean break with no migration -- pre-release, no production data.
Output: Updated Scope enum and all 16+ source files and test files that reference it.
</objective>

<execution_context>
@/Users/dwamianm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dwamianm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.4-namespace-config-expansion/03.4-CONTEXT.md
@.planning/phases/03.4-namespace-config-expansion/03.4-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/prme/types.py (CURRENT -- to be modified):
```python
class Scope(str, Enum):
    PERSONAL = "personal"
    PROJECT = "project"
    ORG = "org"               # RENAME to ORGANISATION = "organisation"
```

From src/prme/ingestion/extraction.py line 55-58 (scope prompt section):
```python
# Lines to update in EXTRACTION_SYSTEM_PROMPT:
#    - "personal" — about a specific individual's preferences...
#    - "project" — about a specific project...
#    - "org" — about organization-wide policies...
# Must add: agent, system, sandbox descriptions
```

From src/prme/ingestion/schema.py lines 25-31 and 62-67:
```python
# ExtractedEntity.scope field description references 'org'
# ExtractedFact.scope field description references 'org'
# Both need "organisation" + new scope values
```

Files referencing Scope.ORG (from grep):
- src/prme/types.py:48 (definition)
- tests/test_retrieval_filter_forwarding.py:292 (usage)

Files referencing "org" as scope string in prompts:
- src/prme/ingestion/schema.py:29, 65
- src/prme/ingestion/extraction.py:58
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand Scope enum to 6 members and update LLM prompts</name>
  <files>src/prme/types.py, src/prme/ingestion/extraction.py, src/prme/ingestion/schema.py</files>
  <behavior>
    - Scope enum has 6 members: PERSONAL, PROJECT, ORGANISATION, AGENT, SYSTEM, SANDBOX
    - Scope.ORG no longer exists (AttributeError if accessed)
    - Scope("organisation") returns Scope.ORGANISATION
    - Scope("agent"), Scope("system"), Scope("sandbox") all resolve correctly
    - Extraction system prompt includes all 6 scope values with descriptions
    - ExtractedEntity.scope and ExtractedFact.scope field descriptions list all 6 scope values
  </behavior>
  <action>
1. **src/prme/types.py** -- Replace the Scope enum:
   - Rename `ORG = "org"` to `ORGANISATION = "organisation"` (RFC-0004 exact match)
   - Add `AGENT = "agent"` with docstring: "Per-agent working memory"
   - Add `SYSTEM = "system"` with docstring: "System-generated content (summaries, organizer output)"
   - Add `SANDBOX = "sandbox"` with docstring: "Temporary isolated scope; supports HARD_DELETE expiry action (RFC-0004 S7)"
   - Update the class docstring to describe all 6 types (use the docstring from RESEARCH.md Pattern 1)
   - No migration aliases -- clean break per CONTEXT.md decision

2. **src/prme/ingestion/extraction.py** -- Update EXTRACTION_SYSTEM_PROMPT:
   - Change line 58: `"org"` -> `"organisation"` and keep description "about organization-wide policies, structures, or shared context"
   - Add after the organisation line:
     - `"agent"` — about a specific AI agent's working memory or internal reasoning
     - `"system"` — system-generated content such as summaries or organizer output
     - `"sandbox"` — temporary or experimental context intended for isolated testing

3. **src/prme/ingestion/schema.py** -- Update ExtractedEntity.scope and ExtractedFact.scope field descriptions:
   - ExtractedEntity.scope (line 28-31): Change `'org' for organization-wide context` to `'organisation' for organization-wide context, 'agent' for agent working memory, 'system' for system-generated content, 'sandbox' for temporary/testing context`
   - ExtractedFact.scope (line 64-67): Change `'personal', 'project', or 'org'` to `'personal', 'project', 'organisation', 'agent', 'system', or 'sandbox'`
  </action>
  <verify>
    <automated>cd /Users/dwamianm/Sites/prism && python -c "from prme.types import Scope; assert len(Scope) == 6; assert Scope.ORGANISATION.value == 'organisation'; assert Scope.AGENT.value == 'agent'; assert Scope.SYSTEM.value == 'system'; assert Scope.SANDBOX.value == 'sandbox'; print('Scope enum: OK')" && python -c "from prme.ingestion.extraction import EXTRACTION_SYSTEM_PROMPT; assert 'organisation' in EXTRACTION_SYSTEM_PROMPT; assert 'agent' in EXTRACTION_SYSTEM_PROMPT; assert 'sandbox' in EXTRACTION_SYSTEM_PROMPT; print('Prompts: OK')"</automated>
  </verify>
  <done>Scope enum has 6 members matching RFC-0004 S3. LLM prompts reference all 6 scope values. No Scope.ORG exists.</done>
</task>

<task type="auto">
  <name>Task 2: Update all Scope.ORG references across source and test files</name>
  <files>tests/test_retrieval_filter_forwarding.py</files>
  <action>
1. **tests/test_retrieval_filter_forwarding.py** line 292:
   - Change `scope=Scope.ORG` to `scope=Scope.ORGANISATION`

2. **Verify no remaining references** -- Run grep across the entire src/ and tests/ directories for:
   - `Scope.ORG` (should find zero matches)
   - `"org"` as a standalone scope value in Python strings (should find zero matches in scope-related contexts)
   - `'org'` as a standalone scope value (same check)

3. **Run the full test suite** to confirm no breakage. Key tests to pass:
   - `test_retrieval_filter_forwarding.py` (has the Scope.ORG reference)
   - `test_contradiction_modeling.py` (uses Scope.PERSONAL, should be unaffected)
   - `test_retrieval_scoring.py` (should be unaffected)

NOTE: Most source files only import Scope and use Scope.PERSONAL as defaults -- they do NOT reference Scope.ORG directly. The only code-level reference to Scope.ORG outside types.py is in test_retrieval_filter_forwarding.py:292. The string references to "org" are in the LLM prompts handled in Task 1.
  </action>
  <verify>
    <automated>cd /Users/dwamianm/Sites/prism && python -m pytest tests/ -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>Zero references to Scope.ORG or "org" as a scope value remain in src/ or tests/. All existing tests pass.</done>
</task>

</tasks>

<verification>
1. `python -c "from prme.types import Scope; print([s.value for s in Scope])"` outputs all 6 values
2. `grep -rn "Scope\.ORG" src/ tests/` returns zero matches
3. `grep -rn "'org'" src/prme/ingestion/` returns zero matches for scope-related strings
4. `python -m pytest tests/ -x -q` passes all tests
</verification>

<success_criteria>
- Scope enum has exactly 6 members: PERSONAL, PROJECT, ORGANISATION, AGENT, SYSTEM, SANDBOX
- The string value for ORGANISATION is "organisation" (not "org")
- All LLM extraction prompts and schema descriptions reference all 6 scope values
- No code references to Scope.ORG or "org" as a scope value remain in the codebase
- All existing tests pass without modification beyond the Scope.ORG rename
</success_criteria>

<output>
After completion, create `.planning/phases/03.4-namespace-config-expansion/03.4-01-SUMMARY.md`
</output>
