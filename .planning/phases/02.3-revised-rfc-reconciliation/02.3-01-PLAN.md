---
phase: 02.3-revised-rfc-reconciliation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - STOR-01
  - STOR-02
  - STOR-03
  - STOR-04
  - STOR-05
  - STOR-06
  - STOR-07
  - STOR-08
  - INGE-01
  - INGE-02
  - INGE-03
  - INGE-04
  - INGE-05
  - RETR-01
  - RETR-02
  - RETR-03
  - RETR-04
  - RETR-05
  - RETR-06
  - ORGN-01
  - ORGN-02
  - ORGN-03
  - ORGN-04
  - ORGN-05
  - INTG-01
  - INTG-02
  - INTG-03
  - INTG-04
  - INTG-05
  - TRST-01
  - TRST-02
  - TRST-03
  - TRST-04
  - TRST-05

must_haves:
  truths:
    - "Every Revised RFC (0000-0014) has been reviewed and any new or changed requirements are captured in REQUIREMENTS.md"
    - "Existing v1 requirements (STOR, INGE, RETR, ORGN, INTG, TRST) are updated in-place with RFC source references and change notes where the revised spec differs from the original"
    - "New requirement domains have new prefixes (EPIS, NSPC, CTXP, DECY, CONF, FDBK, TEMP, MAGT, BRCH, INTN, PORT) with inline RFC source references"
    - "v2/future requirements (Tier 4 RFCs) are added to the v2 Requirements section"
    - "The two original RFCs have been cross-referenced against the Revised suite to confirm nothing was dropped without replacement"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Complete reconciled requirements with RFC traceability"
      contains: "RFC-0"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "docs/RFC-0001-Core-Data-Model.md"
      via: "inline source references (RFC-0001 §N.N)"
      pattern: "RFC-000[0-9]"
---

<objective>
Reconcile REQUIREMENTS.md against the full Revised RFC suite (RFC-0000 through RFC-0014) plus the two original RFCs. Update existing requirements in-place with change notes. Add new requirements with proper prefixes and inline RFC source references.

Purpose: The Revised RFC suite was added after Phases 1-2 were planned and built. Current REQUIREMENTS.md was derived from the original spec documents. The Revised RFCs introduce significant new concepts (epistemic modeling, expanded namespace, decay/forgetting, confidence evolution, feedback loops, intent memory, branching, federation) not captured in current requirements. This plan ensures REQUIREMENTS.md is the single source of truth for what PRME must implement, traceable to specific RFC sections.

Output: Fully updated `.planning/REQUIREMENTS.md` with all RFC-derived requirements, source references, and change notes.
</objective>

<execution_context>
@/Users/dwamianm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dwamianm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02.3-revised-rfc-reconciliation/02.3-CONTEXT.md
@.planning/phases/02.3-revised-rfc-reconciliation/02.3-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update existing requirements and add Tier 0-1 requirements (RFC-0000 through RFC-0004)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Read all 5 RFCs in order: RFC-0000-Suite-Overview.md, RFC-0001-Core-Data-Model.md, RFC-0002-Event-Store.md, RFC-0003-Epistemic-State-Model.md, RFC-0004-Namespace-and-Scope-Isolation.md from the docs/ directory.

**Step 1: Add Design Principles section.** After the "Core Value" line in REQUIREMENTS.md, add a new section "## Design Principles" documenting P1-P8 from RFC-0000 with source references. Include the conformance tier model and [HYPOTHESIS] marker convention.

**Step 2: Update existing v1 requirements in-place.** For each existing STOR, INGE requirement:
- Compare against RFC-0001 (data model), RFC-0002 (event store) conformance requirements
- Where the Revised RFC adds, changes, or clarifies the requirement, modify the requirement text in-place and append a change note: "(Updated per RFC-XXXX §N.N)"
- Key updates per RESEARCH.md findings:
  - STOR-01: Add note about stream, actor_id, actor_type, sequence_num, prev_hash fields from RFC-0001 §3 and RFC-0002 §2
  - STOR-02: Add note about Entity as separate first-class model per RFC-0001 §5, and INTENT node type per RFC-0001 §4
  - STOR-03: Add note about created_by on edges per RFC-0001 §6, and ABOUT_ENTITY edge type
  - STOR-06: Add note about namespace_id replacing simple scope per RFC-0004 §2
  - STOR-07: Note lifecycle state terminology change (TENTATIVE/STABLE vs ACTIVE/DEPRECATED) — FLAG this for manual review per CONTEXT.md locked decision. Do NOT auto-resolve the naming. Add a clearly marked note: "⚠ MANUAL REVIEW: RFC-0001 §4.3 uses ACTIVE/DEPRECATED lifecycle states vs current TENTATIVE/STABLE. See STATE.md decisions."
  - STOR-08: Add note about DEPRECATED state and formal contradiction modeling per RFC-0003 §5

**Step 3: Add new Tier 0-1 requirement domains.** Add new sections after existing v1 requirements:

"### Epistemic State Model" with prefix EPIS:
- EPIS-01: System assigns an epistemic type (OBSERVED, ASSERTED, INFERRED, HYPOTHETICAL, CONDITIONAL, DEPRECATED, UNVERIFIED) to every memory object (RFC-0003 §2)
- EPIS-02: System applies default confidence values from the (epistemic_type, source_type) matrix (RFC-0003 §3)
- EPIS-03: System enforces permitted epistemic transitions and blocks forbidden ones (RFC-0003 §4)
- EPIS-04: System models contradictions by preserving both objects, creating CONTRADICTS edge, and logging CONTRADICTION_NOTED operation (RFC-0003 §5)
- EPIS-05: System supports DEFAULT and EXPLICIT retrieval modes with per-epistemic-type inclusion rules (RFC-0003 §6)
- EPIS-06: System supports CONDITIONAL claims with condition_state tracking (UNKNOWN/TRUE/FALSE/EXPIRED) (RFC-0003 §4.1)

"### Namespace and Scope Isolation" with prefix NSPC:
- NSPC-01: System supports 6 namespace types: PERSONAL, PROJECT, ORGANISATION, AGENT, SYSTEM, SANDBOX (RFC-0004 §2)
- NSPC-02: System models namespaces as first-class entities with id, name, type, parent_id, access_policy, retention_policy, decay_policy_ref (RFC-0004 §2)
- NSPC-03: System enforces granular access permissions (READ, WRITE, ASSERT, DEPRECATE, ADMIN) per actor per namespace (RFC-0004 §3)
- NSPC-04: System supports per-namespace retention policies with TTL, max event count, and expiry actions (RFC-0004 §4)
- NSPC-05: System applies namespace filters before returning vector search candidates (RFC-0004 §5)
- NSPC-06: System logs cross-namespace reference operations (RFC-0004 §6)

Each requirement MUST include an inline source reference in parentheses.

**Step 4: Add cross-cutting requirements.** Add a new section "### Cross-Cutting" with existing or new prefix:
- Add requirement for policy versioning on all policy-driven operations (RFC-0002 §4, RFC-0007, RFC-0008)
- Add requirement for [HYPOTHESIS] parameter configurability (RFC-0000 §4)
- Add requirement for operation logging covering all state changes (RFC-0002 §3)

Use prefix TRST for policy/audit requirements since they naturally belong there, or create a new XCUT prefix if better. Use judgment per Claude's Discretion.
  </action>
  <verify>
Grep REQUIREMENTS.md for "RFC-0000", "RFC-0001", "RFC-0002", "RFC-0003", "RFC-0004" — each must appear at least once.
Grep for "EPIS-" — must find at least 5 entries.
Grep for "NSPC-" — must find at least 5 entries.
Grep for "MANUAL REVIEW" — must find the lifecycle state terminology flag.
  </verify>
  <done>
Existing STOR/INGE requirements are updated in-place with RFC change notes. New EPIS and NSPC requirement domains exist with 5+ requirements each and inline RFC source references. Lifecycle state naming conflict is flagged for manual review. Design principles section exists with RFC-0000 references.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Tier 2-4 requirements and cross-reference original RFCs (RFC-0005 through RFC-0014 + originals)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Read all remaining RFCs: RFC-0005 through RFC-0014 from docs/ directory, plus RFC_0001_PRME_Technical_Spec.md and RFC_0002_RMS_Git_Sync_Profile_v1.md.

**Step 1: Update existing v1 retrieval requirements.** For RETR-01 through RETR-06:
- Compare against RFC-0005 (Hybrid Retrieval Pipeline) and RFC-0006 (Retrieval Cost and Context Efficiency)
- Update in-place with change notes where RFC adds specificity:
  - RETR-03: Add detail about 4 parallel candidate generation paths, path_count signal, 8-input composite scoring formula (RFC-0005 §3-5)
  - RETR-04: Add detail about specific default weights and embedding version mismatch detection (RFC-0005 §5)
  - RETR-05: Add detail about retrieval logging as operations (RFC-0005 §7)
  - RETR-06: Add detail about STR metric, 5 representation levels, 3-priority greedy bin-packing (RFC-0006 §2-5)

**Step 2: Update existing v1 self-organization requirements.** For ORGN-01 through ORGN-05:
- Compare against RFC-0007 (Decay), RFC-0008 (Confidence), RFC-0009 (Feedback Loop)
- Update in-place with change notes:
  - ORGN-01: Add detail about exponential decay function, 5 decay profiles with specific lambda values (RFC-0007 §2-3)
  - ORGN-02: Add detail about lifecycle transition thresholds (salience < 0.30 warning, < 0.10 deprecate, < 0.05 archive), and asymptotic reinforcement formula (RFC-0007 §5, RFC-0008 §2)
  - ORGN-05: Add detail about tombstone format, decay policy versioning (RFC-0007 §7-8)

**Step 3: Add new Tier 2 requirement domains.** Add new v1 sections:

"### Context Packing" with prefix CTXP:
- CTXP-01: System computes Signal-to-Token Ratio (STR) for every retrieval candidate (RFC-0006 §2)
- CTXP-02: System supports 5 representation levels (REFERENCE, KEY_VALUE, STRUCTURED, PROSE, FULL) for memory objects in bundles (RFC-0006 §3)
- CTXP-03: System uses 3-priority greedy bin-packing for context assembly within token budget (RFC-0006 §4)

**Step 4: Add new Tier 3 requirement domains.** Add new v1 sections:

"### Decay and Forgetting" with prefix DECY:
- DECY-01: System applies exponential decay with 5 configurable profiles (PERMANENT, SLOW, MEDIUM, FAST, RAPID) (RFC-0007 §2)
- DECY-02: System decays confidence separately from salience at half the salience decay rate (RFC-0007 §3)
- DECY-03: System supports suppression as a retrieval filter distinct from decay (RFC-0007 §6)
- DECY-04: System schedules organizer jobs at 3 intervals (6hr/daily/weekly) for decay processing (RFC-0007 §7)

"### Confidence Evolution" with prefix CONF:
- CONF-01: System applies asymptotic reinforcement formula: confidence_new = confidence_old + gamma * (1.0 - confidence_old) (RFC-0008 §2)
- CONF-02: System applies multiplicative penalty formula: confidence_new = confidence_old * (1.0 - delta) (RFC-0008 §3)
- CONF-03: System recognizes 4 positive and 4 negative reinforcement signal types with configurable weights (RFC-0008 §4)
- CONF-04: System enforces per-session reinforcement cap of 0.40 and maximum confidence ceiling of 0.97 [HYPOTHESIS] (RFC-0008 §6)
- CONF-05: System checks agent independence before applying cross-agent reinforcement signals (RFC-0008 §5)

"### Feedback Loop" with prefix FDBK:
- FDBK-01: System logs INJECTION_EVENT for every memory bundle injected into context (RFC-0009 §2)
- FDBK-02: System detects feedback signals via hierarchy: explicit user feedback > tool validation > response analysis > behavioral (RFC-0009 §3)
- FDBK-03: System aggregates per-object feedback statistics including usage_ratio (RFC-0009 §4)
- FDBK-04: System monitors 4 quality metrics with alert thresholds for retrieval health (RFC-0009 §5)
- FDBK-05: System prevents circular dependency between feedback signals and retrieval scoring (RFC-0009 §6)

**Step 5: Add Tier 4 requirements to v2 section.** In the existing "## v2 Requirements" section, add:

"### Temporal Pattern Awareness" with prefix TEMP:
- TEMP-01: System detects 8 temporal pattern types (DAILY through ANNUAL, IRREGULAR, DORMANT, BURST) from memory access history (RFC-0010 §2)
- TEMP-02: System applies temporal salience modulation with maximum 0.10 boost [HYPOTHESIS] (RFC-0010 §3)

"### Multi-Agent Memory" with prefix MAGT:
- MAGT-01: System implements domain-scoped trust model for cross-agent memory sharing (RFC-0011 §2)
- MAGT-02: System enforces rate limiting and new-agent quarantine for memory poisoning defense (RFC-0011 §5)

"### Memory Branching" with prefix BRCH:
- BRCH-01: System supports 5 branch types (SIMULATION, COUNTERFACTUAL, PLANNING, SANDBOX, TEST) with isolation guarantees (RFC-0012 §2)
- BRCH-02: System implements selective merge with 3 conflict resolution strategies (RFC-0012 §5)

"### Intent and Goal Memory" with prefix INTN:
- INTN-01: System stores INTENT memory objects with 6 intent types (GOAL, COMMITMENT, OPEN_QUESTION, DECISION_IN_PROGRESS, RISK, FOLLOW_UP) (RFC-0013 §2)
- INTN-02: System tracks intent lifecycle through 6 states (ACTIVE, COMPLETED, CANCELLED, ON_HOLD, EXPIRED, SUPERSEDED) with dependency modeling (RFC-0013 §3)

"### Portability and Federation" — update existing SYNC entries:
- Update SYNC-01 through SYNC-07 to reference RFC-0014 instead of original RFC_0002
- Add SYNC-08: System handles merge conflict resolution with fully specified semantics (RFC-0014 §5)
- Add PORT-01: System supports multiple transport options (Git, HTTPS REST, peer-to-peer) for memory sync (RFC-0014 §3)
- Add PORT-02: System supports federation agreements between PRME instances (RFC-0014 §7)

**Step 6: Cross-reference original RFCs.** Read RFC_0001_PRME_Technical_Spec.md and RFC_0002_RMS_Git_Sync_Profile_v1.md. Verify that nothing was dropped from original specs without replacement in the Revised suite. Per RESEARCH.md findings, the Revised suite is strictly more detailed — note this in a brief comment at the end of REQUIREMENTS.md.

**Step 7: Update traceability table.** Update the traceability table at the bottom of REQUIREMENTS.md:
- Add rows for all new requirement IDs (EPIS, NSPC, CTXP, DECY, CONF, FDBK, TEMP, MAGT, BRCH, INTN, PORT)
- Map each to the appropriate roadmap phase
- Update the Coverage summary counts
  </action>
  <verify>
Grep REQUIREMENTS.md for "RFC-0005" through "RFC-0014" — each must appear at least once.
Grep for "DECY-", "CONF-", "FDBK-", "CTXP-" — each must find entries.
Grep for "TEMP-", "MAGT-", "BRCH-", "INTN-" — each must find entries in v2 section.
Grep for "PORT-" or updated SYNC entries — must reference RFC-0014.
Count total requirement IDs — should be approximately 70-90 (34 original + 40-60 new per RESEARCH.md estimate).
  </verify>
  <done>
All 15 Revised RFCs and 2 original RFCs have been reviewed. REQUIREMENTS.md contains updated existing requirements with change notes, new Tier 1-3 v1 requirement domains (EPIS, NSPC, CTXP, DECY, CONF, FDBK), new Tier 4 v2 requirement domains (TEMP, MAGT, BRCH, INTN, PORT), updated SYNC entries referencing RFC-0014, and a complete traceability table mapping every requirement to a roadmap phase.
  </done>
</task>

</tasks>

<verification>
1. Every Revised RFC (0000-0014) has at least one requirement referencing it via inline source notation
2. Both original RFCs have been cross-referenced (documented in REQUIREMENTS.md)
3. All existing v1 requirements (STOR, INGE, RETR, ORGN, INTG, TRST) retain their IDs — no IDs were removed
4. New requirement domains use proper prefixes: EPIS, NSPC, CTXP, DECY, CONF, FDBK (v1); TEMP, MAGT, BRCH, INTN, PORT (v2)
5. Lifecycle state naming conflict is flagged with "MANUAL REVIEW" marker
6. Traceability table covers all requirement IDs
7. No code changes were made — this is documentation only
</verification>

<success_criteria>
- REQUIREMENTS.md has ~70-90 total requirements (up from 34) covering the full RFC suite
- Every RFC is traceable via inline source references
- Existing requirements are modified in-place with change notes, not duplicated
- v2 section contains Tier 4 requirements
- No RFC is left unreviewed
</success_criteria>

<output>
After completion, create `.planning/phases/02.3-revised-rfc-reconciliation/02.3-01-SUMMARY.md`
</output>
