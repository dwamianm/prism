---
phase: 03.1-epistemic-type-confidence-matrix
plan: 01
subsystem: database
tags: [epistemic, pydantic, duckdb, enum, migration]

# Dependency graph
requires:
  - phase: 01-storage-foundation
    provides: MemoryNode model, DuckDB schema, DuckPGQGraphStore CRUD
  - phase: 03-retrieval-pipeline
    provides: EpistemicType enum, EPISTEMIC_WEIGHTS dict
provides:
  - SourceType enum with 5 provenance categories
  - CREATION_EPISTEMIC_TYPES frozenset (6 valid creation-time types)
  - Native epistemic_type and source_type fields on MemoryNode
  - DuckDB auto-migration for epistemic columns on existing databases
  - Graph store serialization/deserialization of epistemic fields
affects: [03.1-02, ingestion-pipeline, retrieval-pipeline, organizer]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "DuckDB ALTER TABLE migration pattern for new columns (no NOT NULL, DEFAULT only)"
    - "Graceful row fallback in _row_to_node for pre-migration tuples (len check)"

key-files:
  created: []
  modified:
    - src/prme/types.py
    - src/prme/models/nodes.py
    - src/prme/storage/schema.py
    - src/prme/storage/duckpgq_graph.py

key-decisions:
  - "SourceType is a fixed 5-member (str, Enum) for DuckDB VARCHAR compatibility, matching Phase 01 enum pattern"
  - "Defaults ASSERTED/USER_STATED for backward compatibility with existing node creation code"
  - "Graceful _row_to_node fallback via len(row) check handles pre-migration SELECT * results"

patterns-established:
  - "Epistemic field migration: same pattern as _migrate_events_scope (check information_schema, ALTER TABLE with DEFAULT)"

requirements-completed: [EPIS-01]

# Metrics
duration: 3min
completed: 2026-02-21
---

# Phase 03.1 Plan 01: Epistemic Type & Source Type Fields Summary

**SourceType enum and CREATION_EPISTEMIC_TYPES frozenset added to types.py; epistemic_type and source_type as native Pydantic fields on MemoryNode with DuckDB schema migration and graph store column mapping**

## Performance

- **Duration:** 3 min
- **Started:** 2026-02-21T06:07:10Z
- **Completed:** 2026-02-21T06:10:37Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- SourceType enum with 5 members (user_stated, user_demonstrated, system_inferred, external_document, tool_output) for provenance classification
- CREATION_EPISTEMIC_TYPES frozenset with 6 valid creation-time types (DEPRECATED excluded per CONTEXT.md)
- Native epistemic_type and source_type fields on MemoryNode with ASSERTED/USER_STATED defaults
- DuckDB schema migration (_migrate_nodes_epistemic_type) auto-adds columns to existing databases
- Graph store _create_node_sync and _row_to_node updated for full persistence roundtrip

## Task Commits

Each task was committed atomically:

1. **Task 1: Add SourceType enum, CREATION_EPISTEMIC_TYPES, and epistemic fields on MemoryNode** - `b770360` (feat)
2. **Task 2: Add DuckDB schema migration and graph store column mapping** - `1a37177` (feat)

## Files Created/Modified
- `src/prme/types.py` - Added SourceType enum and CREATION_EPISTEMIC_TYPES frozenset
- `src/prme/models/nodes.py` - Added epistemic_type and source_type fields to MemoryNode
- `src/prme/storage/schema.py` - Added _migrate_nodes_epistemic_type() and updated CREATE TABLE DDL
- `src/prme/storage/duckpgq_graph.py` - Updated _create_node_sync() and _row_to_node() for new columns

## Decisions Made
- SourceType is a fixed 5-member (str, Enum) matching Phase 01 pattern for DuckDB VARCHAR compatibility
- Default values ASSERTED/USER_STATED chosen for backward compatibility with existing code that creates nodes without specifying epistemic fields
- Graceful fallback in _row_to_node via `len(row) > 16` check handles pre-migration SELECT * results where new columns are not yet present

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- MemoryNode now carries epistemic_type and source_type natively, ready for confidence matrix lookup (Plan 02)
- Graph store CRUD persists and retrieves both fields through DuckDB, enabling downstream epistemic filtering and scoring

## Self-Check: PASSED

- All 4 modified files exist on disk
- Commit b770360 (Task 1) verified in git log
- Commit 1a37177 (Task 2) verified in git log
- 14/14 existing tests pass
- End-to-end DuckDB roundtrip verified

---
*Phase: 03.1-epistemic-type-confidence-matrix*
*Completed: 2026-02-21*
