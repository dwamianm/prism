---
phase: 03.1-epistemic-type-confidence-matrix
plan: 02
subsystem: database
tags: [epistemic, pydantic, confidence-matrix, ingestion, retrieval, migration]

# Dependency graph
requires:
  - phase: 03.1-epistemic-type-confidence-matrix
    provides: MemoryNode epistemic_type and source_type native fields, DuckDB schema migration
  - phase: 03-retrieval-pipeline
    provides: EpistemicType enum, EPISTEMIC_WEIGHTS, filter_epistemic, scoring pipeline
  - phase: 02-ingestion-pipeline
    provides: IngestionPipeline, ExtractionProvider, ExtractedFact schema
provides:
  - ConfidenceMatrix frozen Pydantic model with (epistemic_type, source_type) -> float lookup
  - DEFAULT_CONFIDENCE_MATRIX singleton with [HYPOTHESIS]-marked values
  - infer_epistemic_type() and infer_source_type() heuristic functions for store() path
  - backfill_epistemic_types() idempotent startup migration
  - ExtractedFact epistemic_type field with strict Pydantic validation
  - LLM extraction prompt with epistemic classification instructions
  - Matrix-based confidence assignment at node creation
  - Direct epistemic_type field access in retrieval (no getattr fallbacks)
  - UNVERIFIED confidence threshold filtering in DEFAULT mode
affects: [organizer, retrieval-pipeline, api]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Frozen Pydantic BaseModel for immutable configuration (ConfidenceMatrix)"
    - "(epistemic_type, source_type) tuple-keyed dict for matrix lookup"
    - "Lazy import in pipeline.py and engine.py for epistemic module (circular dep avoidance)"
    - "Pydantic field_validator for strict LLM output validation (works with instructor retries)"
    - "Startup backfill with metadata markers for idempotent migration"

key-files:
  created:
    - src/prme/epistemic/__init__.py
    - src/prme/epistemic/matrix.py
    - src/prme/epistemic/inference.py
    - src/prme/epistemic/migration.py
  modified:
    - src/prme/ingestion/schema.py
    - src/prme/ingestion/extraction.py
    - src/prme/ingestion/pipeline.py
    - src/prme/storage/engine.py
    - src/prme/retrieval/filtering.py
    - src/prme/retrieval/scoring.py
    - tests/test_retrieval_scoring.py

key-decisions:
  - "OBSERVED/USER_STATED = 0.90 confidence per user decision; all other matrix values [HYPOTHESIS]-marked"
  - "Strict Pydantic field_validator on ExtractedFact epistemic_type -- rejects DEPRECATED at creation, works with instructor retry mechanism"
  - "UNVERIFIED threshold filtering at 0.30 confidence in DEFAULT retrieval mode per RFC-0003 S8"
  - "Backfill marks nodes with _epistemic_backfill metadata for idempotency; preserves existing confidence values"
  - "store() infers epistemic_type and source_type from node_type and role when not explicitly provided"

patterns-established:
  - "Epistemic subpackage pattern: matrix.py (config), inference.py (heuristics), migration.py (backfill)"
  - "Matrix lookup with fallback logging: lookup_with_fallback() warns on missing combinations"
  - "Metadata markers for migration idempotency: _epistemic_backfill flag checked before re-processing"

requirements-completed: [EPIS-02, EPIS-05]

# Metrics
duration: 5min
completed: 2026-02-21
---

# Phase 03.1 Plan 02: Confidence Matrix & Epistemic Wiring Summary

**ConfidenceMatrix with (epistemic_type, source_type) lookup, LLM-classified epistemic types in extraction, matrix-based confidence at creation, direct field access in retrieval, and startup backfill migration**

## Performance

- **Duration:** 5 min
- **Started:** 2026-02-21T06:13:25Z
- **Completed:** 2026-02-21T06:19:02Z
- **Tasks:** 2
- **Files modified:** 11

## Accomplishments
- ConfidenceMatrix frozen Pydantic model with 18 (epistemic_type, source_type) confidence values, OBSERVED/USER_STATED = 0.90 per user decision
- LLM extraction prompt includes epistemic classification instructions; ExtractedFact has strict validation rejecting DEPRECATED
- Ingestion pipeline assigns epistemic_type from LLM output and confidence from matrix lookup; store() infers from context
- Retrieval filtering and scoring use direct field access (zero getattr/setattr fallbacks remain in codebase)
- UNVERIFIED nodes filtered by confidence threshold (0.30) in DEFAULT retrieval mode per RFC-0003 S8
- Startup backfill migration auto-runs, marks nodes with _epistemic_backfill metadata, preserves existing confidence

## Task Commits

Each task was committed atomically:

1. **Task 1: Create epistemic subpackage with ConfidenceMatrix, inference, and migration** - `4468560` (feat)
2. **Task 2: Wire epistemic type through ingestion, store(), extraction, retrieval, and migration startup** - `0a55090` (feat)

## Files Created/Modified
- `src/prme/epistemic/__init__.py` - Public API exports for epistemic subpackage
- `src/prme/epistemic/matrix.py` - ConfidenceMatrix frozen Pydantic model with lookup methods
- `src/prme/epistemic/inference.py` - infer_epistemic_type() and infer_source_type() heuristics
- `src/prme/epistemic/migration.py` - backfill_epistemic_types() idempotent startup migration
- `src/prme/ingestion/schema.py` - Added epistemic_type field with strict Pydantic validator to ExtractedFact
- `src/prme/ingestion/extraction.py` - Added epistemic classification instructions to EXTRACTION_SYSTEM_PROMPT
- `src/prme/ingestion/pipeline.py` - Wired epistemic_type from LLM extraction and matrix confidence at node creation
- `src/prme/storage/engine.py` - store() infers epistemic/source types; create() runs backfill migration
- `src/prme/retrieval/filtering.py` - Direct field access, UNVERIFIED threshold filtering
- `src/prme/retrieval/scoring.py` - Direct node.epistemic_type access (removed getattr fallback)
- `tests/test_retrieval_scoring.py` - Native MemoryNode field construction (removed object.__setattr__ hacks)

## Decisions Made
- OBSERVED/USER_STATED maps to 0.90 confidence per user decision (0.85-0.90 range)
- All other matrix values marked [HYPOTHESIS] per P6 (Empirical Humility) -- tunable per deployment
- Strict Pydantic field_validator on ExtractedFact works with instructor's retry mechanism (max_retries=3) -- LLM gets re-prompted on validation failure
- UNVERIFIED confidence threshold set at 0.30 per RFC-0003 S8 default; configurable via module constant
- Backfill preserves existing confidence values (user decision) -- only epistemic_type is assigned
- store() confidence param changed from default 0.5 to None (derives from matrix when not specified)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Complete epistemic type lifecycle: LLM classification at extraction -> matrix confidence at creation -> native field access at retrieval
- All EPIS-01, EPIS-02, EPIS-05 requirements satisfied -- Phase 03.1 is complete
- Ready for Phase 03.2 (or next gap closure phase) with full epistemic infrastructure in place

## Self-Check: PASSED

- All 11 created/modified files exist on disk
- Commit 4468560 (Task 1) verified in git log
- Commit 0a55090 (Task 2) verified in git log
- 14/14 tests pass
- 0 getattr/setattr fallback patterns remaining in codebase

---
*Phase: 03.1-epistemic-type-confidence-matrix*
*Completed: 2026-02-21*
