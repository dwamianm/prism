---
phase: 03.1-epistemic-type-confidence-matrix
verified: 2026-02-21T06:24:31Z
status: passed
score: 12/12 must-haves verified
re_verification: false
---

# Phase 3.1: Epistemic Type & Confidence Matrix Verification Report

**Phase Goal:** Every memory object carries its epistemic type at creation, default confidence is assigned from a (type, source) matrix, and epistemic filtering uses real field values instead of getattr fallback
**Verified:** 2026-02-21T06:24:31Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #  | Truth | Status | Evidence |
|----|-------|--------|----------|
| 1  | MemoryNode has a native epistemic_type field populated at creation with a valid EpistemicType value | VERIFIED | `MemoryNode.model_fields` includes `epistemic_type: EpistemicType = ASSERTED` — confirmed via live Python import |
| 2  | MemoryNode has a native source_type field with SourceType enum value | VERIFIED | `MemoryNode.model_fields` includes `source_type: SourceType = USER_STATED` — confirmed via live Python import |
| 3  | DuckDB schema includes epistemic_type column, persisted and queryable | VERIFIED | `schema.py` line 82: `epistemic_type VARCHAR NOT NULL DEFAULT 'asserted'` in CREATE TABLE; migration function `_migrate_nodes_epistemic_type` at line 229 called from `initialize_database` at line 359 |
| 4  | Default confidence is assigned from the (epistemic_type, source_type) matrix during ingestion | VERIFIED | `pipeline.py` lines 348-362: lazy imports `DEFAULT_CONFIDENCE_MATRIX`, calls `lookup_with_fallback(fact_epistemic_type, fact_source_type)`, sets `confidence=matrix_confidence` on MemoryNode |
| 5  | Default confidence is assigned from the matrix via store() path | VERIFIED | `engine.py` lines 235-245: infers types then calls `DEFAULT_CONFIDENCE_MATRIX.lookup_with_fallback(epistemic_type, source_type)` |
| 6  | OBSERVED/USER_STATED maps to confidence 0.90 (user decision) | VERIFIED | `matrix.py` line 34: `("observed", "user_stated"): 0.90` |
| 7  | Epistemic filtering (DEFAULT mode) uses real epistemic_type field values, not getattr fallback | VERIFIED | `filtering.py` line 49: `epistemic_type = candidate.node.epistemic_type` — direct field access with inline comment confirming intentional design |
| 8  | Epistemic scoring uses real epistemic_type field values, not getattr fallback | VERIFIED | `scoring.py` line 49: `epistemic_weight = EPISTEMIC_WEIGHTS.get(node.epistemic_type, 0.7)` — direct field access |
| 9  | Zero getattr/setattr fallback patterns for epistemic_type remain in codebase | VERIFIED | `grep -rn "getattr.*epistemic_type\|setattr.*epistemic_type\|__setattr__.*epistemic_type"` returned 0 results across src/ and tests/ |
| 10 | ExtractedFact epistemic_type field has strict Pydantic validation rejecting DEPRECATED | VERIFIED | `schema.py` lines 80-99: `field_validator("epistemic_type")` rejects "deprecated" — confirmed via live Python test |
| 11 | Existing nodes are backfilled at startup via idempotent migration | VERIFIED | `migration.py` `backfill_epistemic_types()` exists and is called from `engine.py` lines 167-174 on every engine creation |
| 12 | All 14 existing tests pass after changes | VERIFIED | `python -m pytest tests/ -x -q` returned `14 passed, 1 warning` |

**Score:** 12/12 truths verified

### Required Artifacts

#### Plan 01 Artifacts

| Artifact | Provides | Status | Details |
|----------|----------|--------|---------|
| `src/prme/types.py` | SourceType enum, CREATION_EPISTEMIC_TYPES set | VERIFIED | `class SourceType(str, Enum)` at line 82 with 5 members; `CREATION_EPISTEMIC_TYPES: frozenset` at line 151 with 6 members (DEPRECATED excluded) |
| `src/prme/models/nodes.py` | epistemic_type and source_type fields on MemoryNode | VERIFIED | Lines 40-47: both fields declared as native Pydantic `Field()` with EpistemicType/SourceType type annotations and defaults |
| `src/prme/storage/schema.py` | DuckDB migration for epistemic_type and source_type columns | VERIFIED | `_migrate_nodes_epistemic_type(conn)` at line 229; called from `initialize_database` at line 359; CREATE TABLE includes both columns at lines 82-83 |
| `src/prme/storage/duckpgq_graph.py` | Column mapping in _row_to_node and _create_node_sync | VERIFIED | `_create_node_sync` includes `epistemic_type` and `source_type` in INSERT at lines 367-391; `_row_to_node` reads `row[16]`/`row[17]` with graceful fallback at lines 986-987 |

#### Plan 02 Artifacts

| Artifact | Provides | Status | Details |
|----------|----------|--------|---------|
| `src/prme/epistemic/__init__.py` | Public API exports | VERIFIED | Exports `ConfidenceMatrix`, `DEFAULT_CONFIDENCE_MATRIX`, `infer_epistemic_type`, `infer_source_type` |
| `src/prme/epistemic/matrix.py` | ConfidenceMatrix frozen Pydantic model | VERIFIED | `class ConfidenceMatrix(BaseModel)` with `ConfigDict(frozen=True)`, 18 matrix entries, `lookup()` and `lookup_with_fallback()` methods, `DEFAULT_CONFIDENCE_MATRIX` singleton |
| `src/prme/epistemic/inference.py` | Epistemic type inference from node context | VERIFIED | `infer_epistemic_type()` and `infer_source_type()` heuristic functions covering SUMMARY→INFERRED, EVENT→OBSERVED, other→ASSERTED |
| `src/prme/epistemic/migration.py` | Startup backfill for existing nodes | VERIFIED | `async def backfill_epistemic_types(graph_store, engine_conn=None)` — idempotent (checks `_epistemic_backfill` metadata marker), logs EPISTEMIC_TYPE_ASSIGNED |
| `src/prme/ingestion/schema.py` | epistemic_type field on ExtractedFact with strict validation | VERIFIED | Lines 69-99: `epistemic_type: str = Field(default="asserted", ...)` plus `@field_validator("epistemic_type")` that rejects DEPRECATED |
| `src/prme/retrieval/filtering.py` | Direct epistemic_type field access without getattr | VERIFIED | Line 49: `epistemic_type = candidate.node.epistemic_type` — direct access; UNVERIFIED threshold filtering at lines 58-71 |
| `src/prme/retrieval/scoring.py` | Direct epistemic_type field access without getattr | VERIFIED | Line 49: `EPISTEMIC_WEIGHTS.get(node.epistemic_type, 0.7)` — direct access |

### Key Link Verification

#### Plan 01 Key Links

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `src/prme/models/nodes.py` | `src/prme/types.py` | `import EpistemicType, SourceType` | WIRED | Line 13: `from prme.types import EpistemicType, LifecycleState, NodeType, SourceType` |
| `src/prme/storage/duckpgq_graph.py` | `src/prme/models/nodes.py` | `_row_to_node maps DB columns to MemoryNode fields` | WIRED | Lines 986-1007: reads `row[16]`/`row[17]` for epistemic/source fields, passes to `MemoryNode(... epistemic_type=raw_epistemic, source_type=raw_source)` |
| `src/prme/storage/schema.py` | DuckDB nodes table | `ALTER TABLE migration adds columns` | WIRED | Line 249: `ALTER TABLE nodes ADD COLUMN epistemic_type VARCHAR DEFAULT 'asserted'`; called via `initialize_database` at line 359 |

#### Plan 02 Key Links

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `src/prme/ingestion/pipeline.py` | `src/prme/epistemic/matrix.py` | `Matrix lookup for default confidence at node creation` | WIRED | Lines 348-362: lazy import `DEFAULT_CONFIDENCE_MATRIX`, calls `lookup_with_fallback()`, assigns result as `confidence=matrix_confidence` on MemoryNode |
| `src/prme/ingestion/extraction.py` | LLM extraction prompt | `Epistemic type classification in system prompt` | WIRED | Lines 61-67: `EXTRACTION_SYSTEM_PROMPT` section 7 lists "observed", "asserted", "inferred", "hypothetical", "unverified" with examples |
| `src/prme/storage/engine.py` | `src/prme/epistemic/inference.py` | `Infer epistemic type for store() path` | WIRED | Lines 235-245: lazy import `infer_epistemic_type`, `infer_source_type`, called before matrix lookup |
| `src/prme/epistemic/migration.py` | `src/prme/storage/duckpgq_graph.py` | `Query and update existing nodes` | WIRED | Lines 66-131: queries `SELECT id, node_type, metadata, epistemic_type FROM nodes`, runs `UPDATE nodes SET epistemic_type = ? ...` directly via DuckDB connection |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| EPIS-01 | 03.1-01 | System assigns an epistemic type (OBSERVED, ASSERTED, INFERRED, HYPOTHETICAL, CONDITIONAL, DEPRECATED, UNVERIFIED) to every memory object at creation (RFC-0003 §3) | SATISFIED | `MemoryNode.epistemic_type` native field with `EpistemicType` enum; default ASSERTED; store() infers from node_type; ingestion pipeline assigns from LLM extraction; REQUIREMENTS.md line 229 marks EPIS-01 Complete |
| EPIS-02 | 03.1-02 | System applies default confidence values from the (epistemic_type, source_type) matrix, with values tunable per deployment [HYPOTHESIS] (RFC-0003 §4) | SATISFIED | `ConfidenceMatrix` frozen Pydantic model in `matrix.py` with 18 (type, source) entries; `lookup_with_fallback()` used in both ingestion pipeline and `store()` path; REQUIREMENTS.md line 230 marks EPIS-02 Complete |
| EPIS-05 | 03.1-02 | System supports DEFAULT and EXPLICIT retrieval modes with per-epistemic-type inclusion rules — HYPOTHETICAL and DEPRECATED excluded from DEFAULT mode (RFC-0003 §8) | SATISFIED | `filter_epistemic()` in `filtering.py` uses direct `candidate.node.epistemic_type` field access; DEFAULT mode excludes `HYPOTHETICAL` and `DEPRECATED` via `DEFAULT_EXCLUDED_EPISTEMIC` set; UNVERIFIED filtered below 0.30 confidence threshold; REQUIREMENTS.md line 233 marks EPIS-05 Complete |

No orphaned requirements — all three EPIS requirements declared in plan frontmatter are present in REQUIREMENTS.md and have evidence of implementation.

### Anti-Patterns Found

No blocker or warning anti-patterns detected. The following matches were false positives:

- `duckpgq_graph.py` lines 439, 700, 806: `placeholders` variable refers to SQL `?` parameter placeholders, not stub/placeholder code.

### Human Verification Required

None — all four success criteria are verifiable programmatically:

1. MemoryNode native field existence: confirmed via `MemoryNode.model_fields`.
2. DuckDB schema columns: confirmed via DDL inspection and migration function.
3. Matrix-based confidence assignment: confirmed via code trace through pipeline.py and engine.py.
4. Direct field access in retrieval: confirmed via code inspection and zero grep matches for getattr/setattr fallbacks.

### Gaps Summary

No gaps. All phase goals are achieved:

- Every memory object carries its epistemic type at creation via the native `MemoryNode.epistemic_type` Pydantic field.
- Default confidence is assigned from the `(epistemic_type, source_type)` matrix at creation in both the ingestion pipeline path (LLM-classified) and the `store()` path (heuristic-inferred).
- Epistemic filtering in `filtering.py` and scoring in `scoring.py` use direct `node.epistemic_type` field access — no `getattr` fallbacks remain anywhere in the codebase.
- The `backfill_epistemic_types` migration runs idempotently at engine startup to retrofit existing nodes.
- All 14 tests pass with no regressions.

---

_Verified: 2026-02-21T06:24:31Z_
_Verifier: Claude (gsd-verifier)_
