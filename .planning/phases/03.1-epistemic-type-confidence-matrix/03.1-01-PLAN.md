---
phase: 03.1-epistemic-type-confidence-matrix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/prme/types.py
  - src/prme/models/nodes.py
  - src/prme/storage/schema.py
  - src/prme/storage/duckpgq_graph.py
autonomous: true
requirements: [EPIS-01]

must_haves:
  truths:
    - "MemoryNode has a native epistemic_type field with EpistemicType enum value"
    - "MemoryNode has a native source_type field with SourceType enum value"
    - "DuckDB nodes table includes epistemic_type and source_type VARCHAR columns"
    - "Existing databases are auto-migrated to add the new columns with sensible defaults"
    - "Graph store CRUD correctly persists and retrieves epistemic_type and source_type"
  artifacts:
    - path: "src/prme/types.py"
      provides: "SourceType enum, CREATION_EPISTEMIC_TYPES set"
      contains: "class SourceType"
    - path: "src/prme/models/nodes.py"
      provides: "epistemic_type and source_type fields on MemoryNode"
      contains: "epistemic_type"
    - path: "src/prme/storage/schema.py"
      provides: "DuckDB migration for epistemic_type and source_type columns"
      contains: "_migrate_nodes_epistemic_type"
    - path: "src/prme/storage/duckpgq_graph.py"
      provides: "Column mapping for new fields in _row_to_node and _create_node_sync"
      contains: "epistemic_type"
  key_links:
    - from: "src/prme/models/nodes.py"
      to: "src/prme/types.py"
      via: "import EpistemicType, SourceType"
      pattern: "from prme\\.types import.*SourceType"
    - from: "src/prme/storage/duckpgq_graph.py"
      to: "src/prme/models/nodes.py"
      via: "_row_to_node maps DB columns to MemoryNode fields"
      pattern: "epistemic_type.*row\\[1[67]\\]"
    - from: "src/prme/storage/schema.py"
      to: "DuckDB nodes table"
      via: "ALTER TABLE migration adds columns"
      pattern: "ALTER TABLE nodes.*ADD COLUMN.*epistemic_type"
---

<objective>
Add epistemic_type and source_type as first-class fields on MemoryNode with full DuckDB persistence and graph store integration.

Purpose: EPIS-01 requires every memory object to carry an epistemic type at creation. This plan adds the field to the data model, persists it to DuckDB, and wires it through the graph store CRUD layer so that downstream plans (confidence matrix, ingestion wiring, retrieval cleanup) can use the native field.

Output: MemoryNode with epistemic_type and source_type fields, DuckDB schema migration, updated graph store serialization.
</objective>

<execution_context>
@/Users/dwamianm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dwamianm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-epistemic-type-confidence-matrix/03.1-CONTEXT.md
@.planning/phases/03.1-epistemic-type-confidence-matrix/03.1-RESEARCH.md

# Key source files
@src/prme/types.py
@src/prme/models/nodes.py
@src/prme/storage/schema.py
@src/prme/storage/duckpgq_graph.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SourceType enum and CREATION_EPISTEMIC_TYPES set to types.py, add epistemic_type and source_type fields to MemoryNode</name>
  <files>src/prme/types.py, src/prme/models/nodes.py</files>
  <action>
In `src/prme/types.py`:

1. Add a `SourceType` enum using the `(str, Enum)` pattern (per Phase 01 decision for DuckDB VARCHAR compatibility):
   - USER_STATED = "user_stated" -- Explicit user statement
   - USER_DEMONSTRATED = "user_demonstrated" -- User behavior/action
   - SYSTEM_INFERRED = "system_inferred" -- System-generated inference
   - EXTERNAL_DOCUMENT = "external_document" -- From uploaded/external doc
   - TOOL_OUTPUT = "tool_output" -- From tool/API result
   Fixed enum with 5 values per RESEARCH.md recommendation. Place it near the other domain enums (after Scope or LifecycleState).

2. Add a `CREATION_EPISTEMIC_TYPES` frozenset containing the types valid at creation time: {OBSERVED, ASSERTED, INFERRED, HYPOTHETICAL, CONDITIONAL, UNVERIFIED}. DEPRECATED is excluded -- it is a lifecycle transition only, not assignable at creation (per CONTEXT.md locked decision). Place it near the existing EPISTEMIC_WEIGHTS dict.

In `src/prme/models/nodes.py`:

1. Import `SourceType` from `prme.types` (EpistemicType should already be importable from types.py since Phase 03 added it).

2. Add two fields to MemoryNode:
   - `epistemic_type: EpistemicType = Field(default=EpistemicType.ASSERTED, description="Epistemic classification of this memory object (RFC-0003 S3)")`
   - `source_type: SourceType = Field(default=SourceType.USER_STATED, description="Source provenance type for confidence matrix lookup (RFC-0003 S4)")`

   Place these fields logically near `confidence` and `salience` since they affect scoring. Default to ASSERTED/USER_STATED for backward compatibility with existing code that creates nodes without specifying these fields.

Note: MemoryNode currently has ConfigDict(frozen=False), so the new fields are mutable -- this is correct for the ingestion pipeline which sets values after construction in some paths.
  </action>
  <verify>
Run `python -c "from prme.types import SourceType, EpistemicType, CREATION_EPISTEMIC_TYPES; print(list(SourceType)); print(CREATION_EPISTEMIC_TYPES)"` to confirm enum and set are importable.

Run `python -c "from prme.models.nodes import MemoryNode; from prme.types import EpistemicType, SourceType; n = MemoryNode(node_type='entity', user_id='u1', content='test'); print(n.epistemic_type, n.source_type)"` to confirm fields exist with defaults.
  </verify>
  <done>
SourceType enum has 5 members, CREATION_EPISTEMIC_TYPES frozenset has 6 members (DEPRECATED excluded), and MemoryNode has epistemic_type and source_type fields with defaults of ASSERTED and USER_STATED respectively.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add DuckDB schema migration and update graph store column mapping</name>
  <files>src/prme/storage/schema.py, src/prme/storage/duckpgq_graph.py</files>
  <action>
In `src/prme/storage/schema.py`:

1. Add a `_migrate_nodes_epistemic_type(conn)` function following the exact pattern of `_migrate_events_scope()`:
   - Check `information_schema.columns` for whether `epistemic_type` column exists on `nodes` table
   - If missing, run two ALTER TABLE statements:
     - `ALTER TABLE nodes ADD COLUMN epistemic_type VARCHAR DEFAULT 'asserted'` (no NOT NULL -- DuckDB 1.4.x limitation per Phase 02.1 decision)
     - `ALTER TABLE nodes ADD COLUMN source_type VARCHAR DEFAULT 'user_stated'`
   - Log migration result with structlog

2. Update the `CREATE TABLE nodes` DDL to add both columns at the END of the column list (after `updated_at`):
   - `epistemic_type VARCHAR NOT NULL DEFAULT 'asserted'` (NOT NULL is fine in CREATE TABLE, just not ALTER TABLE)
   - `source_type VARCHAR NOT NULL DEFAULT 'user_stated'`

3. Call `_migrate_nodes_epistemic_type(conn)` from `create_schema()` after the existing `_migrate_events_scope()` call.

In `src/prme/storage/duckpgq_graph.py`:

1. Update `_create_node_sync()` to include epistemic_type and source_type in the INSERT statement:
   - Add `epistemic_type` and `source_type` to the column list and VALUES placeholders
   - Pass `node.epistemic_type.value` and `node.source_type.value` (using .value since DuckDB stores VARCHAR)

2. Update `_row_to_node()` to read the new columns:
   - The new columns are at indices 16 (epistemic_type) and 17 (source_type) since they are appended to the end
   - Add these to the dict passed to MemoryNode.model_validate():
     - `"epistemic_type": row[16] if len(row) > 16 else "asserted"` (graceful fallback for pre-migration rows)
     - `"source_type": row[17] if len(row) > 17 else "user_stated"`
   - The existing model_validate will coerce the string values to their respective enums

3. If there is an explicit column SELECT list anywhere (like `_EVENT_COLUMNS` pattern from Phase 02.1), update it to include the new columns. Otherwise verify `SELECT *` is used and the new columns will be included automatically.
  </action>
  <verify>
Run `python -c "
import asyncio, tempfile, os
from prme.storage.engine import MemoryEngine
from prme.config import PRMEConfig
from prme.types import EpistemicType, SourceType, NodeType

async def test():
    with tempfile.TemporaryDirectory() as d:
        cfg = PRMEConfig(storage_path=d)
        engine = await MemoryEngine.create(cfg)
        # store() creates a node
        ev = await engine.store('test epistemic', user_id='u1')
        # Verify the node roundtrips through DuckDB
        nodes = await engine._graph_store.get_nodes(user_id='u1')
        for n in nodes:
            print(f'epistemic_type={n.epistemic_type}, source_type={n.source_type}')
        await engine.close()

asyncio.run(test())
"` to confirm end-to-end persistence and retrieval of new fields.

Run existing tests: `cd /Users/dwamianm/Sites/prism && python -m pytest tests/ -x -q 2>&1 | tail -20` to confirm no regressions.
  </verify>
  <done>
DuckDB nodes table has epistemic_type and source_type columns. New databases create them via CREATE TABLE with NOT NULL. Existing databases get them via ALTER TABLE migration. Graph store _create_node_sync persists the values, _row_to_node deserializes them back to MemoryNode with correct enum types. All existing tests pass.
  </done>
</task>

</tasks>

<verification>
1. `python -c "from prme.types import SourceType, CREATION_EPISTEMIC_TYPES; assert len(list(SourceType)) == 5; assert len(CREATION_EPISTEMIC_TYPES) == 6"` -- types are correct
2. `python -c "from prme.models.nodes import MemoryNode; n = MemoryNode(node_type='entity', user_id='u1', content='x'); assert hasattr(n, 'epistemic_type'); assert hasattr(n, 'source_type')"` -- fields exist natively (no getattr hack)
3. `python -m pytest tests/ -x -q` -- all existing tests pass (backward compatible)
4. End-to-end: store a node, read it back, verify epistemic_type and source_type survive DuckDB roundtrip
</verification>

<success_criteria>
- MemoryNode.epistemic_type is a native Pydantic field with EpistemicType default
- MemoryNode.source_type is a native Pydantic field with SourceType default
- DuckDB schema has both columns on the nodes table
- Migration runs automatically for existing databases
- Graph store CRUD correctly persists and retrieves both fields
- All existing tests pass without modification
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-epistemic-type-confidence-matrix/03.1-01-SUMMARY.md`
</output>
