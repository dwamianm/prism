# Opt-in interaction successor — registered repair, 2026-09-22

This is a separately identified successor authorized by the user's instruction
to implement the fixes and continue until meaningful results. The original
failed study, partial answers, registration inconsistency and report remain
unchanged. No default change, merge, global flag deployment or push is authorized.
Production code remains `a66ee854325890c6bc28f6b515efeb6ed7df4deb`.

## Repair and comparison units

The unchanged official step-by-step reader prompt exhausted the original 1,024
output allowance. Use a fixed 8,192-output-token allowance for every successor
baseline and treatment, leaving the 64-token judge allowance, prompts, official
substring scoring rule and 4,096-memory-token budget (100 caller reserve) intact.
The larger ceiling is headroom, not a required response length. Run and retain
two repetitions of eight authored temporal/update/count/abstention fixtures plus
sixteen positive/negative judge controls before any successor dataset answer.
All must complete; report their observed quality separately. DeepSeek through
Ollama, temperature 0, seed 42, 65,536 model context, `think=false` and the pinned
digest remain as previously amended. A hosted alias is not immutable weights.
This protocol is not a matched GPT or original-output-budget comparison.

All 500 checksum-pinned LongMemEval-S cases remain development data. No selected
subset may produce a primary score. The unit of fail-closed execution is now an
entire registered arm: a terminal failure invalidates its answer comparison,
retains all records, and stops launching additional cases in that arm. Other
predeclared arms still run, including after unfavorable or unavailable results.
Every arm's disposition must appear in the report. A complete authenticated
control/candidate pair can be analyzed without waiting for an unrelated arm.
This rule is fixed before any successor answers; it does not rehabilitate v1.
No failed logical response is regenerated or replaced. The existing four-attempt
transient-HTTP retry limit is unchanged; all attempts are retained.

## Two ingestion strata

Retrieval comparisons use the same 500 authenticated immutable historical packs,
with private copies for each arm. Baseline, episode routing, augmentation,
projection alone (added to identify its interaction), episode+augmentation,
episode+projection, reranker, reformulation, reranker+reformulation, temporal
relations, and temporal+episode all run. Models, dependencies and assets stay
fixed. Historical ingestion cost is disclosed as historical.

Fresh ingestion comparisons include a fresh no-flag control, supersedence, QA
pairing, surprise gating and the full-feature exploratory arm. Every case builds
an empty pack by calling the actual sequential `MemoryEngine.store` for every
nonempty original source turn. No question, reference answer or evidence label
enters store(). All original roles, order, source metadata and event times stay.
No bulk adapter, extracted claims or historical graph copying is introduced.

Research-only constructor wrappers supply otherwise implicit UUIDs and admission
clocks: UUID5 of fixed namespace/question/source-position/turn/kind and a fixed
2026-09-22 UTC admission origin plus turn ordinal microseconds. Event, source
and additional QA nodes have separate kinds. Each fresh arm therefore receives
the same raw IDs/clocks, while store hooks run normally; explicit model values
are never overwritten. Production files and model/provider behavior are unchanged.
This removes random tie-break/admission-time confounding but must be disclosed
as benchmark fixture construction, not normal wall-clock ingestion timing.
Physical elapsed ingestion time, full source count, QA nodes, novelty metadata,
superseded state and hook invocations are recorded. Ingestion timings are summed
question wall time under the registered workload, not a serving SLA.

Balanced packing is already the production baseline. The three requested
ingestion+balanced labels alias their individual observations and are not extra
replicates or factorial interactions. Projection and augmentation remain mutually
exclusive; the exploratory full arm uses augmentation. Best-combination creation
waits for complete results for all eight individuals and their matched controls.

Five memory tasks and four reader/judge slots per process remain the execution
limits. Historical and fresh stages may be separate processes; any overlapping
host/provider load must be reported, and those timings cannot claim isolated
serving performance. Cold and immediate warm retrieval are retained separately;
the first context always feeds the reader. Model-backed repeated differences
remain observations, never a choice of the more favorable output.

## Integrity and activation

Freeze this protocol, executor, analyzer, tests, current dependency versions,
production source hashes and configurations before dataset calls. Authenticate
exact contexts, token counts, request bindings, complete input/output packs and
durable replayable receipts. Never open historical master packs for writes.
Log swallowed feature failures. Observe returned reformulations without changing
them, count actual ingestion hooks, and read ranking/episode/evidence operations
and temporal outcomes from receipts. An enabled but inactive feature is an
applicability finding, not evidence of useful treatment. For identical contexts,
separately report answer disagreements as reader/judge variation.

Temporal relations retain the documented answer-blind resolver and Jev protocol,
options, repair bound, digest and 0.85 gate. Provider failures invalidate the arm;
safe semantic rejections are ordinary observations. Product alignment/Jev remains
the separate explicit pair/review workflow, never a matrix flag.

## Analysis and next experiments

For each fully authenticated complete arm report accuracy/category totals,
all paired wins/losses, source omissions, conflict flags, context tokens,
ingestion time, cold/warm p50/p95, and provider usage/failures. Unknown dollar
costs and unavailable metrics stay null. Failed arms receive no partial score.
Use the existing 10,000 paired bootstrap draws (seed 20260922) for question and
full-history-cluster 95% intervals; the source-only preflight found 500 distinct
session-overlap components, so no shared-source cluster is split here. Use exact
two-sided discordant-pair tests and Holm adjustment across every registered
non-control/non-exploratory unique comparison, retaining failed comparisons in
the family as unavailable (p=1). This is inspected development evidence.

Error diagnostics separate missing annotated sources among returned candidates,
sources dropped by packing, and wrong answers despite complete annotated source
coverage. Annotation presence is not proof of reader fault or semantic adequacy;
manually examine representative errors and preserve ambiguous annotations.
Diagnostic labels never enter ingestion, retrieval or temporal providers.

Select at most two individual arms only after all eight have valid complete
comparisons: positive lower cluster interval, Holm p<.05, no category/abstention
loss, no new complete-evidence loss, zero terminal failures. Sort by gain,
retrieval p95, then ID. With fewer than two eligible arms, declare the combination
an alias of the eligible arm or baseline. Do not invent a favorable combination.
Mechanistic changes motivated by completed development errors need separately
frozen follow-up comparisons retaining every earlier result.

Next benchmark order remains Banking, EventQA, Conflict, Detective, then BEAM
and MemoryArena only after the earlier stages complete. Their exact task inputs,
adapters, controls and scoring require registration before inference. Ingestion
hooks bypassed by a native adapter require a separate matched direct-store stratum.
An untouched confirmation requires unused source families, independently checked
against all prior experiments; these 500 cases cannot supply it. Without that
confirmation and installed/DuckDB/PostgreSQL/API/restart/isolation regressions,
no development result can support a production-default recommendation.
